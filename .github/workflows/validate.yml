name: Validate documentation
on:
  pull_request:
  push:
    branches: [master, main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r requirements-preview.txt
      - run: python scripts/docs.py build --lang all --version all
      - run: python scripts/check_site.py --lang all --version all
      - run: python scripts/docs.py build --production
      - run: python scripts/check_site.py --production
      - uses: actions/upload-artifact@v4
        with:
          name: developer-docs-preview
          path: site/
      - uses: actions/upload-artifact@v4
        with:
          name: developer-docs-production
          path: site-production/
