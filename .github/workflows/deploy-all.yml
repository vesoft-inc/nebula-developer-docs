name: Legacy deployment disabled (all)
# Old enterprise deployment is intentionally retired; use validate.yml to build.
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use validate.yml for the new bilingual preview. Public hosting is not configured yet."
